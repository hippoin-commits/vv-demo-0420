---
description: 演示数据、占位文案、截图还原 UI 中的人物姓名：一律用张三/李四/王五区分角色，禁止用截图或真实姓名。
globs: "**/*.{tsx,ts,jsx,js,md,mdc}"
alwaysApply: true
---

# 演示人名约定

在**演示数据、Mock、占位用户、从设计稿/截图还原的聊天或表单**里需要出现多个可区分的自然人姓名时：

1. **统一使用**「**张三**」「**李四**」「**王五**」及其合理变体（如「张三 (我)」、审批流里再加「王五」），只要能区分不同人即可。
2. **禁止**把截图、设计稿或需求文档里的**真实姓名**写进仓库代码或长期文案（避免误用他人姓名、也避免与真实环境混淆）。
3. 新增或修改上述场景时，先检索是否已有 `DEMO_*`、常量或同一卡片内命名，**优先复用同一套占位名**，保持同一条演示链路里人名一致。
4. 同一屏需要多于三个自然人时，在姓名上增加**简短角色后缀**区分即可（如「张三（协作）」「李四（审批）」），仍须以张/李/王为姓名字面主体，勿再引入其它姓氏占位。
